import { StockTypeDirective } from './stock-type.directive';

describe('StockTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new StockTypeDirective();
    expect(directive).toBeTruthy();
  });
});
